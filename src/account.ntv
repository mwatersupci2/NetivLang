module account

type Account struct {
    id: u64
    balance: i64
}

fn transfer(from: mut Account, to: mut Account, amount: i64) -> Result {
    if from.balance >= amount {
        from.balance -= amount
        to.balance += amount
    }
}
